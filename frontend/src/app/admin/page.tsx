import { RoutePlaceholder } from "@/components/rentflow/route-placeholder";

export default function AdminOverviewPage() {
  return (
    <RoutePlaceholder
      title="Admin Overview"
      description="Static system overview shell for admin operators."
      activePath="/admin"
    />
  );
}
